{
  "name": "tfg-motion-studio-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for the TFG Motion Studio — author, preview, and export brand videos from Claude Code without the browser UI.",
  "scripts": {
    "build": "node build.mjs",
    "start": "node dist/server.mjs"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "playwright-core": "^1.49.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
